A polished modern macOS application icon, 1024x1024, centered, in the contemporary macOS Big Sur / Sonoma style: a rounded-rectangle "squircle" app tile with a smooth glossy surface, subtle top-to-bottom gradient, soft realistic drop shadow beneath the tile, and gentle inner highlight for depth.

Subject (single clear central glyph): the canonical Markdown mark — a bold rounded-rectangle badge containing a capital letter "M" next to a downward-pointing arrow — rendered as the focal element. Integrate a subtle "Quick Look / preview" motif: the markdown mark sits on a small white document card that peeks out behind it showing a few faint horizontal lines of rendered text (a heading line and body lines), suggesting a rendered preview. Optionally a very subtle magnifier/peek hint.

Color & style: tasteful developer-tool aesthetic in the quality tier of Linear, Vercel, and Raycast app icons. Squircle background is a refined deep indigo-to-blue gradient; the Markdown M-arrow glyph is crisp clean white. High contrast so the glyph reads clearly. Flat, geometric, minimal, no clutter, no text or wordmark anywhere, no letters other than the single "M" in the markdown mark. Crisp vector-like edges, pixel-perfect, legible at both large and small sizes. Centered composition with even padding. Plain neutral background around the squircle.
